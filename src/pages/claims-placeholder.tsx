import { AlertCircle } from "lucide-react";
import { PlaceholderPage } from "@/features/navigation";

export default function ClaimsPlaceholder() {
  return <PlaceholderPage title="Réclamations" icon={AlertCircle} />;
}
